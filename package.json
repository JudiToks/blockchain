{
  "name": "mini-projet-crowdfunding",
  "version": "1.0.0",
  "description": "Mini-projet blockchain: Crowdfunding on-chain",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.7"
  }
}
